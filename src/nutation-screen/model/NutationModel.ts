/**
 * NutationModel.ts — placeholder until Screen 2 is implemented.
 */

import type { TModel } from "scenerystack/joist";

export class NutationModel implements TModel {
  public reset(): void {
    // Placeholder — Screen 2 will reset nutation state here.
  }

  public step(_dt: number): void {
    // Placeholder — Screen 2 integrator will advance state here.
  }
}
